import { RxConceptPipe } from './rx-concept.pipe';

describe('RxConceptPipe', () => {
  it('create an instance', () => {
    const pipe = new RxConceptPipe();
    expect(pipe).toBeTruthy();
  });
});
